# CLAUDE.md

先读并遵守 @AGENTS.md（拆收件箱的约定正本）和 @CONTRIBUTING.md（改代码的约定）。
用 `@` 而不是「请去读」：实测 Claude Code 不会自动加载 `AGENTS.md`，`@` 才真把它内联进来。

本文件不复述它们的任何内容，只留一件在别的工具上不成立的事：怎么排定期回顾。

## 定期分析怎么排

**拆收件箱不用排**——服务自己会做：收件箱一有没处理的条目、安静一段时间就自动
`POST /api/expand`，spawn 的提示词是「读 AGENTS.md 和 workflows/expand.md，
处理收件箱里还没处理的条目」，跟网页上「立即拆解」按钮走的是同一条。默认开着，
延迟在设置里改。

**要排的是回顾**（AGENTS.md 那节「定期分析」说的事：过期没做完的、在 `doing` 里
躺很久的、反复出现的同类任务）。它没有任何自动触发，服务不会替他排节奏。

（**想立刻跑一次不用排**：网页上「回顾」那一屏底部有一颗「让 AI 回顾一遍」，
走 `POST /api/review`，跟下面这两条路是同一份提示词。这一节说的是**定期**。）

（**只想看一个项目也不用排**：侧栏那份清单的 `⋯` 里「让 AI 回顾这份清单」，
同一条路带一个 `listId`。想排定期的按清单回顾，把范围那句话追加在下面两条的
提示词后面就行——措辞照 `workflows/review.md`「叫你的那句话里带了范围」那节。）

两条路：

- `/schedule` 建一个定时的云端会话，提示词写「读这个仓库的 AGENTS.md 和
  workflows/review.md，回顾一遍现有任务」
- 或者本机的计划任务定时跑一次 `claude -p "读 AGENTS.md 和 workflows/review.md，
  回顾一遍现有任务"`

**别把它排得太密。** 跟拆解不一样：收件箱空的时候拆解什么都不用做，代价接近零；
而回顾每次都会把 `data/tasks/` 下所有任务读一遍、想一遍，任务没变化也照样烧掉
一两分钟和一次订阅额度。一天一次到一周一次是合理区间，具体由他定。

（**按清单那种便宜得多**，因为它只读那一份清单的任务——接口模式下省的是实打实的
token。但也别因此排得太密：省的是每次的量，不是次数本身的代价。）

回顾产出的是**建议**，挂在对应的任务卡上等他点，不会直接改任务——形状和字段
白名单在 AGENTS.md「提修改建议」一节。
