# AGENTS.md

这是一个**任务系统**。人在网页上随手丢文本进收件箱，**你**把它读走、拆成任务。

数据在 `data/` 下——「一实体一文件」：`data/tasks/`、`data/inbox/`、`data/proposals/`、
`data/lists/`、`data/folders/`、`data/insights/` 各是一个目录，目录里每个文件是
一条记录（`<id>.json`）。**你只写一类文件：`data/outbox-<unique>.json`。**
`data/tasks/` 和 `data/inbox/` 你只读、不写——这两类目录唯一的写者是服务本身，
网页上的增删改、到点提醒、还有你拆出来的任务，全部经它落盘。这样切是因为拆解一次要
一分半钟，这中间用户在网页上点掉、删掉的任何东西，都不该被你收工时的一次整体回写覆盖掉。

服务会用两份固定提示词叫起你（正本在 `server/src/expand.ts` 的 `PROMPT`）：

- 「读 AGENTS.md 和 workflows/expand.md，处理收件箱里还没处理的条目」——用户点了
  「立即拆解」，或者收件箱安静下来之后服务自己按设置里的延迟自动叫的
  （默认开，见 `workflows/expand.md`）。
- 「读 AGENTS.md 和 workflows/review.md，回顾一遍现有任务」——用户在「回顾」那一屏
  点了「让 AI 回顾一遍」。**这一份没有自动触发**：回顾每次都要把所有任务
  读一遍，任务没变化也照样烧掉一两分钟和一次额度，节奏由他定。

同一时刻只会有一份在跑（两者共用服务端那把单飞锁）。提示词里写了该读哪个
工作流，你不用再去猜是谁触发的、也不要顺手把另一件也干了。

（**要改这个仓库的代码**而不是拆任务，看 `CONTRIBUTING.md`。）

## 会话一开始就做的事

**读 `data/inbox/` 目录下的文件，看有没有 `processed: false` 的条目。有就主动提出
拆解，别等他问。**

一条都没有就正常回答他的问题，不要把这件事变成每句话前的仪式。

## 文件

| 文件 | 你能做什么 |
|---|---|
| `data/inbox/<id>.json` | **只读。** 他随手丢进来的原始文本，一条一个文件。`processed: false` 的是还没拆的 |
| `data/tasks/<id>.json` | **只读。** 任务，一条一个文件，出现在网页看板上。你拆出来的新任务不写在这里 |
| `data/lists/<id>.json` | **只读。** 清单。要给任务填 `listId` 之前先读这里看有哪些，对不上就写 `null`，别瞎猜 |
| `data/folders/<id>.json` | **只读。** 清单的文件夹（两层分组，装的是 `List`，不是 `Task`），拆任务用不上，知道有这回事就行 |
| `data/proposals/<id>.json` | **只读。** 你以前提过、他还没处理的修改建议。重跑分析之前先看这份，别把同一个意见换个措辞再提一遍。写它的唯一途径是 outbox 的 `updates`，见下面 |
| `data/countdowns/<id>.json` | **别碰。** 倒数纪念日（「距离考试还有 30 天」）。它不是任务——没有「做完」这一步、不进「今天」、不提醒，是他自己在网页上填的一份日子清单。拆解和回顾都用不上它 |
| `data/insights/<id>.json` | **只读。** 你以前提过的跨任务观察。`dismissedAt` 非空的是他点过「知道了」的，别再提。写它的唯一途径是 outbox 的 `insights`，见下面 |
| `data/outbox-<unique>.json` | **只写，也只写这一类。** 拆解结果/修改建议/观察，形状见下面。服务发现它出现，几百毫秒内同步合并进对应目录，再删掉它 |
| `data/meta.json`、`data/trash/`、`data/*.json.v1` | **别碰。** `meta.json` 是迁移版本号；`trash/` 是被删除的任务真正落地的地方（软删除，服务本身在维护）——你不用读它，也别去改它，`data/tasks/` 里少了一条多半是他删掉了，正常现象，不代表数据丢了；`inbox.json.v1`/`tasks.json.v1`/`proposals.json.v1` 是首次把旧格式（三个大数组）迁移成现在这套「一实体一文件」时留下的原始备份，不会自动清掉 |

现在只有 outbox 文件还是**数组**（`[]`）；`inbox`/`tasks`/`proposals`/`lists`/
`folders`/`insights` 每一类都是一个目录，目录里一个文件对应一条记录，**每个文件
顶层是一个对象**，不是数组。`data/` 不在版本库里，新 clone 下来是空的——那几个目录
如果都不存在，等服务先起一次，它会自己铺好空目录（不是空文件），你不用替它建。

**文件名自己挑一个唯一的**，例如带上当前时间戳：`outbox-20260811-153602.json`，或者
随手拼一段随机字符：`outbox-a1b2c3.json`——只要不跟目录里其它还没被处理的 outbox 文件
重名就行。**不是固定名字 `outbox.json` 了**：这个文件夹里随时可能有另一个你（网页上
点了「立即拆解」触发的那个进程，或者别的终端会话）在同时写，撞了同一个文件名就会互相
覆盖，谁也不知道自己那份被谁盖掉了。各写各的文件名，服务会把它们都扫到、按文件名顺序
依次合并，谁都不挡谁。

**原子写：先写一个临时文件名，写完整再改名成正式的 `outbox-<unique>.json`。**
比如先写 `outbox-a1b2c3.json.tmp`，写完调用改名/移动，落成 `outbox-a1b2c3.json`。
直接对着最终文件名写的话，服务的文件监听器有极小概率在你写到一半时就发现了它、读到
半截 JSON——先写临时名再改名，改名这一步在文件系统里是原子的，监听器只会看到「文件
突然完整出现」，不会看到半成品。

**临时文件名本身不能长得像 `outbox-<unique>.json`。** 监听器认 outbox 文件靠的是
「`outbox-` 开头、`.json` 结尾」这个形状，不管它是不是写完整了。上面的例子
`outbox-a1b2c3.json.tmp` 是安全的，因为它以 `.tmp` 结尾，不满足这个形状；如果改成
`outbox-a1b2c3-tmp.json` 这种以 `.json` 收尾的临时名，监听器会把它当成一个真的
outbox 文件去读，读到的就是你还没写完的半截内容。随便用什么后缀都行，只要不是
`outbox-` 开头又 `.json` 结尾就行。

**整个文件一次写完整的 JSON**，不要追加半截、不要写坏格式——每次都是整个覆盖，没有
「追加」这回事。

**合并需要服务在跑。** 写 outbox 文件本身随时能做，不需要服务开着；但只有服务的
文件监听器发现它、校验通过，任务才会真的合并进看板——服务重新启动时也会补做一次这个
检查（顺带重试上次因为校验失败而留在磁盘上的坏文件），所以「写完之后服务才开」也没
问题，不会丢，等它下次启动就会补上。**写文件这个动作本身就是触发合并的信号**：只要
服务当时开着，你写完的那一刻它就会去处理，不需要再等别的什么。
**能确认服务开着（网页打得开）就放心汇报「已经拆好了」；确认不了就提醒他「启动服务之后我拆的这些才会出现」。**

## outbox-<unique>.json 的形状

```json
[
  {
    "inboxId": "收件箱条目的 id",
    "tasks": [ { 完整任务对象 }, { 完整任务对象 } ]
  }
]
```

数组里一项对应一条收件箱记录；一条记录可以拆出 0 个（判断成「这不是任务」，`tasks`
给空数组）到多个任务。

**还有第二种条目形态：`updates`**——对**已有任务**提修改建议；**还有第三种：
`insights`**——跨任务的观察，不挂在任何一条任务或收件箱记录上，见下面「第三种条目：
insights」一节。三种可以放在同一个文件里，但**必须是数组里各占一项，不是一个条目里
塞两个键**：

```json
[
  { "inboxId": "…", "tasks": [ … ] },
  {
    "updates": [
      {
        "id": "已有任务的 id",
        "patch": { "reminders": [{ "at": "2026-08-20T01:00:00.000Z", "firedAt": null }] },
        "reason": "一句话说清为什么建议这么改"
      }
    ]
  }
]
```

**一个条目里同时出现 `tasks` / `updates` / `insights` 中的不止一个，会让整个文件
被拒收。** 别写成 `{"inboxId": …, "tasks": […], "updates": […]}`——服务没法判断
你想干哪件事，与其猜错、把其中一半静默丢掉，不如退回来让你改对。拆解结果、修改建议、
观察各占数组里的一项。

**`updates` 不会直接改任务**，会变成一条「建议」挂在那张卡上，等他点「接受」或
「忽略」。这是有意的：你直接改的话，改完之后卡片上那个时间是谁定的就说不清了。
详细规矩见下面「提修改建议」一节。

## 拆解要做的事

读一条 inbox 文本，产出一个或多个任务对象，装进上面形状里的 `tasks` 数组，`inboxId`
填那条收件箱记录的 `id`。**不要碰 `data/tasks/`，也不要碰 `data/inbox/`**——服务合并
的时候会自己把 `data/inbox/` 里那条标成 `processed: true`、把新任务的 id 填进
`taskIds`，这两步都不用你做。

任务对象的字段：

```json
{
  "id": "用 uuid",
  "title": "一句话说清要做什么",
  "notes": "补充说明，markdown，没有就空字符串（怎么写见下面「notes 写成 markdown」）",
  "status": "todo",
  "due": "2026-08-15T18:00:00.000Z 或 null",
  "startAt": "什么时候才开始能做，2026-09-10T01:00:00.000Z 或 null（多数任务写 null）",
  "endAt": "结束时刻，跟 startAt 一起构成「时间段」；多数任务写 null",
  "persistentReminder": false,
  "reminders": [{ "at": "2026-08-15T09:00:00.000Z", "firedAt": null }],
  "subtasks": [{ "text": "能单独做完的一步", "done": false }],
  "source": "ai",
  "aiComment": "一句话说清你为什么这么拆",
  "createdAt": "现在的 ISO 时间",
  "updatedAt": "同上",
  "order": null,
  "listId": "某个清单的 id，或者 null",
  "section": null,
  "tags": ["标签"],
  "priority": 0,
  "repeat": null,
  "completedAt": null,
  "postponeCount": 0,
  "waitingFor": "在等谁/等什么，没有就 null",
  "context": "computer / out / home / contact / easy 五选一，没把握就 null",
  "attachments": [],
  "focusSessions": [],
  "estimateMinutes": null,
  "habit": false,
  "pinned": false,
  "reviewedAt": null,
  "parentId": null
}
```

几条约定：

- **`status` 合法值是 `"todo"` / `"doing"` / `"done"` / `"later"` / `"abandoned"`
  五个之一**，
  别的字符串（哪怕是「进行中」这种看起来对的中文，或者 `"pending"`）看板不认——
  不会报错，但那张卡会落进待办列，卡片上会挂一个红色「状态异常：xxx」标签，
  看着不像一张正常的待办卡。**但你只能写 `"todo"`**——`"later"`（搁置：暂时
  不想看见它，还会回来）和 `"abandoned"`（放弃：决定不做了，但不删掉、以后
  回顾还看得见）都是人的决定，跟「完成」一样，是他自己在网页上点出来的，
  不是你能替他判断的事。写了这两个中的任何一个，会让这一整个 outbox 文件
  校验不过、原样留在 `data/` 里，见下面「校验失败会怎样」。`"doing"`/`"done"` 理论上也不该由你写（拆解阶段任务还没
  开始做），但校验不会拦，正常流程里也用不上。
- **`order` 一律写 `null`**。这是任务在「今天」视图里的手动排序位置，只有人在
  网页上拖动/上下移动之后才会有具体数字——不管你写不写这个字段、写成什么，
  服务合并的时候都会强制改成 `null`，不会报错，也不会告诉你被改了，写了纯属
  白费。
- **`due` 和 `reminders` 各管各的，别搞反**：`due` 只决定看板上那个红色
  「已过期」标签，不会触发任何提醒；**只有 `reminders` 里的时刻会触发提醒**
  （网页横幅、Windows 通知、webhook 三路都看它）。写了 `due` 没写 `reminders`
  的任务，到了截止时间只会变红，不会通知任何人。
- **`reminders` 里每一条的 `firedAt` 一律写 `null`。** 那是服务发完提醒之后
  自己盖的章，你写了等于让这条提醒不再发出。
- **`priority` 一律写 `0`。** 哪件事更重要是他的判断，跟 `order` 同一类——
  你不能替他定。**但你可以在 `updates` 里建议改它**，那条路他点了才算数。
- **`estimateMinutes` 一律写 `null`（或者不写）。** 「这件事要花多久」是他的
  判断，跟 `priority` 同一类——你不知道他做这类事有多快，估出来的数字他一眼
  就能看出不靠谱，反而不如空着。写了不拒收，服务端悄悄改回 `null`。
- **`section` 一律写 `null`（或者不写）。** 那是他给自己清单分的段
  （「准备」「收尾」这种），怎么分是他的组织习惯，跟 `pinned`/`parentId`
  同一类。你拆出来的那几条本来就靠「按来源」聚在一起，再塞进某个分段是替他
  决定这份清单该怎么摆，而你连他现在有哪几段都不该假设。写了不拒收，服务端
  悄悄改回 `null`。
- **`reviewedAt` 一律写 `null`（或者不写）。** 那是他在「回顾」那一屏点
  「看过了」时盖的章，盖了之后那条卡住的项目七天内不再出现在他的清单上。
  你盖这个章等于替他做了那个决定——而回顾正是你在跑的事，等于让你有办法把
  自己该报的东西藏起来。写了不拒收，服务端悄悄改回 `null`。
- **`stuckNote` 字段已经没了，不用写。** 它曾经是「他自己写的现场笔记」，
  但界面上从来没有过写它的地方，而 `waitingFor`（在等谁）把那件事接走了。
  你要是按老约定带了一把 `"stuckNote": null`，服务端会接住并丢掉，不拒收。
- **`pinned` 一律写 `false`（或者不写）。** 置顶是「我现在最想先看见这条」，
  跟 `order`/`priority` 同一类纯粹的意志表达，不是你能替他判断的事。跟那两个
  一样，写了不会让文件被拒收，服务端会悄悄改回 `false`。
- **`parentId` 一律写 `null`（或者不写）。** 多级任务是他自己在网页上挂的。
  你拆一句话出来的那几条任务本来就靠「按来源」表达「同出一源」——再给它们
  一套父子关系，是同一件事的第二种说法，而两种说法迟早对不上。同样是悄悄
  改回 `null`，不拒收。
- **`completedAt` 写 `null`、`postponeCount` 写 `0`、`focusSessions` 和
  `attachments` 写 `[]`。** 服务端自己盖章、自己数。
- **`repeat`、`tags`、`waitingFor`、`habit`、`context` 你可以写**——那些是原文里的
  事实，不是判断。「每周一写周报」是 `repeat`，「等张老师回邮件」是 `waitingFor`，
  「每天喝八杯水」是 `habit: true` 加上每天重复，「一周健身三次」是 `habit: true`
  加上 `every: "week"` + `weekdays: [1, 3, 5]`（**次数就是选中的天数**）。
- **`context`（情境）只在原文真的限定了条件时才写，别猜**。五档：
  `computer`（得坐在电脑前）、`out`（得出门）、`home`（得在家）、
  `contact`（得找人：打电话、发消息）、`easy`（累的时候也干得了）。
  「去银行办卡」是 `out`，「给房东打个电话」是 `contact`。而「写周报」不是
  `computer`——它确实得在电脑前写，但原文没说这件事，是你推的。
  **推不出来就写 `null`**：一个猜错的情境比没有情境更坏——他按「外出」
  筛出一屏准备出门办的事，里面混一条只能在电脑前干的，这份清单就不能
  直接照着做了，而那正是它全部的价值。
- **`repeat` 的字段形状**（不重复就整个字段写 `null`）：
  ```json
  { "every": "week", "interval": 1, "weekdays": [1], "until": null }
  ```
  `every` 是 `"day"` / `"week"` / `"month"` / `"year"` / `"ebbinghaus"` 五选一，
  必须写。最后那个是艾宾浩斯记忆法（背单词那种，间隔按遗忘曲线越拉越长），
  原文明确在说「记忆/复习/背」才用它，别拿它当「每隔几天」的花哨写法。
  `interval`（每几个 `every` 重复一次）、`weekdays`（星期几，0=周日……6=周六，
  只有 `every: "week"` 时才有意义）、`until`（重复到什么时候为止，没有就
  `null`）、`from`（下一次从哪儿算起，见下一条）、`count`（还要再重复几次，
  见下下条）、`step`（艾宾浩斯走到第几步，**你一律不写**，见下面）、
  `monthDay`（月重复锚在几号，见下面）这七个
  **可以不写**，缺了会落
  默认值 `interval: 1`、`weekdays: []`、`until: null`、`from: "due"`、
  `count: null`、`step: 0`、`monthDay: null`——不是
  「不写就整个 `repeat` 校验不过」。「每天」的例子（`weekdays`
  对 `every: "day"` 没有意义，留 `[]` 或者干脆不写）：
  ```json
  { "every": "day", "interval": 1 }
  ```
  写了的话类型还是要对：`interval` 得是正整数，`weekdays` 里每个数字得是
  0-6 之间的整数，`until` 得是合法的 ISO 时间或者 `null`，`from` 得是
  `"due"` / `"done"` 之一，`count` 得是非负整数或者 `null`，`step` 得是非负
  整数，`monthDay` 得是 1-31 的整数或者 `null`——这几种照样会让整个 outbox
  文件被拒收，见下面「校验失败会怎样」。
- **`repeat.monthDay`：月重复锚在几号。** 只有 `every: "month"` 时有意义，
  跟 `weekdays` 是一对。**拆到「每月 15 号交房租」这种明确说了几号的才写**，
  同时把 `due` 也落在下一个 15 号；没说几号的（「每月盘一次货」）不写，
  服务端会在第一次生成下一条时用当时的截止日期把它补上。
  没有它，「每月 31 号」过一次二月就会永久漂成 28 号。
- **`repeat.step` 一律不写**（或者写 `0`）。那是艾宾浩斯「已经复习到第几次」
  的计数，服务端每生成一条新实例自己加一——你拆解的是一条**还没开始**的任务，
  它的步数只能是 0。
- **`repeat.from`：`"due"`（默认）还是 `"done"`。** 「下一次从哪儿算起」——
  `"due"` 从这一条的截止时间往后推，周期不会漂（「每周一写周报」拖到周三
  才写完，下一次还是下周一）；`"done"` 从**完成的那一刻**往后推（「每三天
  健身一次」周五耽搁了、周六才做，下一次是周二不是周一）。**拿不准就别写**，
  落默认的 `"due"`。判据是原文本身：说的是「每周一 / 每月 1 号」这种钉在
  日历格子上的事，用 `"due"`；说的是「每隔三天 / 每两周一次」这种「离上次
  做完过了多久」的事，用 `"done"`。
- **`repeat.count`：还要再重复几次**（不含当前这一条），`null`（默认）是
  一直重复。**存的是剩余次数，不是总次数**——「背这组单词复习五次」写
  `count: 4`（这一条加后面四条）。原文没说次数就别写，落默认的 `null`；
  说了「就这一次」写 `0`。
- **`habit: true` 必须配 `repeat.every === "day"` 或 `"week"`。** 别的组合会让整个
  outbox 文件校验不过——习惯是「每天做」或者「一周做几次」，没有「每月打卡」
  这种东西。而且**必须跟 `repeat` 一起写**：只写 `habit`、不写 `repeat` 也拒收，
  因为校验器只看得见这一份 patch，判断不了原任务的重复档是什么。
- **`listId` 要先读 `data/lists/` 看有哪些清单**，对不上就写 `null`，别瞎猜。
- **`id` 必须是新生成的 uuid，不能复用已经存在的任务的 id**，哪怕是你自己之前
  拆出来的那个。服务发现某个 id 已经在 `data/tasks/` 里，会直接丢掉这条、只在
  日志里提一句，不会覆盖原任务，也不会报错给你看——`PATCH`/`DELETE` 都是按 id
  找任务的，两张卡共用一个 id 会导致改一个改到另一个、删一个把两个都删了。
  这类冲突只有你这边能避免，服务只能做到不让它伤到原任务。
  **`id` 里不能出现 `/`、`\` 或 `..`**——这种值不是合法的 uuid，服务会把它当成
  路径穿越，整个 outbox 文件连同同一批里其它正常的任务一起被拒收（校验失败，
  见下面「校验失败会怎样」），不会悄悄把它换成一个新 id 继续往下走，也不会
  只丢这一条、放过其它任务。横幅会点名是哪个任务的 `id` 有问题，换一个干净的
  uuid 重新提交这份文件就行。
- **`source` 一律写 `"ai"`**。`"user"` 是他在网页上点「新任务」自己建的那种，
  服务会自己填，不用你管——你只要别把自己拆出来的任务标成 `"user"`。
  这个字段决定卡片上「截止/提醒」那两个键名用不用群青（群青只标 AI 写的和
  AI 推断的），标错了等于替他认领了他自己填的时间。
- **`aiComment` 必填一句**。他要能看出你为什么这么拆——拆错了他才知道该怎么纠正你。
- **`notes` 写成 markdown，不是一段流水句子。** 网页那边是按 markdown 渲染的
  （标题/列表/待办项/引用/表格/代码块/链接都认，样式也都画了），而这个字段
  一直被当成「再补一句话」用，于是那套渲染在真实数据上一次都没派上用场。
  **有结构就用结构**：
  - 几步要做的 → `- ` 列表；有先后 → `1. ` 有序列表
  - 引用他原话、或者引用某个约束 → `> `
  - 参考链接 → `[标题](url)`，别把裸 URL 扔在句子中间
  - 对照关系（口径/负责人/期限这种成对的东西）→ 表格
  - 命令、路径、字段名 → 行内反引号
  **可以多段**，这个字段没有一句话的限制——`aiComment` 才是「一句」。
  但**别为了用格式而用**：真的只有一句补充说明时，就写那一句。
  跟 `subtasks` 分工清楚：**能单独做完、他要一条条勾掉的步骤放 `subtasks`**
  （那是数据模型，勾了会存下来）；`notes` 里的 `- [ ]` 只是文本，点不动。
  同一批步骤别在两处各写一遍。
- **`subtasks` 每一条的 `text` 认行内格式**：反引号（命令/路径/字段名）、
  `**粗体**`、`[标题](url)` 都会渲染出来，写了不会变成一串符号。**只有行内**
  ——那一行文字要跟勾选框并排，标题/列表/表格这些块级结构写了会把它挤走，
  真要结构就放 `notes`。这一条不改上面那个分工：`subtasks` 仍然是「一条条勾掉
  的步骤」，只是那一步的文字可以写得准确一点（「跑 `npm run report -- --q3`」
  比「跑那个报表命令」有用）。
- **时间一律 ISO 8601 带时区**（`2026-08-15T09:00:00.000Z`）。写成「下周三」的话提醒永远不会触发，
  而且不会报错——解析不了的 `reminders` 时刻会被静默跳过。
- **`startAt` 只在原文真的说了「那之前做不了」时才写**，别的一律 `null`。
  它回答的是「什么时候**才**能开始」，不是「打算什么时候做」——后者是 `due`。
  该写的样子：「等新电脑到了再装环境」（到货日）、「开学后去交表」（开学那天）、
  「下个月一号才开放报名」（一号）。**不该写的样子**：「这周有空就做」
  （那是没有截止时间，不是做不了）、「先放着」（那是他自己搁置，轮不到你替他定）。
  写了它的任务，界面上会显示「9 月 10 日开始」，而且在那之前不进「推荐任务」
  ——所以**写错的代价是一条任务凭空消失在他眼前一个月**，拿不准就留 null。
- **没有明确时间就推断一个合理的**，别一律留 null——留 null 等于这个任务永远不会提醒他。
  推断的理由写进 `aiComment`，他不同意可以自己改。**推断出来的时间既要写进 `due`
  也要写进 `reminders`**（加一条 `{ "at": 推断出来的时间, "firedAt": null }`，
  除非你确实只想标一个截止日期、不想提醒他）——只写 `due` 不写 `reminders`
  是最容易踩的坑，看起来像是设好了提醒，实际永远不会响。
- **一条文本可以拆成多个任务**，也可以判断成「这不是任务」——那种情况这条记录对应的
  `tasks` 给空数组 `[]`，`inboxId` 照填。服务合并的时候会照样把 `data/inbox/` 里那条
  标成 `processed: true`（`taskIds` 留空），你只要在回复里说明为什么这么判断。

## 提修改建议（`updates`）

定期分析看出「这条该改期」「这条拆得太粗」「这条卡了很久」的时候，用这个形态提出来。

- **`patch` 里只能出现这几个字段：`title` / `notes` / `due` / `startAt` /
  `reminders` / `subtasks` / `tags` / `listId` / `repeat` / `priority` /
  `waitingFor` / `context`。**（不写个数：这份名单刚因为写死了「十个」而飘过
  一次——`startAt` 加进 `PROPOSABLE` 时这里没跟上，于是文档在把一个实际允许的
  字段说成禁的。正本是 `server/src/task.ts` 的 `PROPOSABLE`。）
  出现别的会让**整个 outbox 文件校验不过**，跟 `status:"later"` 同一条路径。
  - `status` / `order`：完成与否、先做哪个，是纯粹的意志表达，不是能拿来
    「建议」的东西
  - `source`：改了它等于篡改「这条是谁写的」，双色墨水整套都靠它
  - `aiComment`：拆解当时的记录，是历史事实，不该被后来的分析改写
  - `completedAt` / `postponeCount` / `focusSessions`：服务端自己盖章、自己数
  - `habit` / `attachments`：前者是他怎么看待这条任务，不是能替他判断的事；
    后者你不产生文件

  注意 **`priority` 在这份白名单里，虽然拆解时你不能直接写它**（`toTask` 会
  强制归 `0`）。这不矛盾：拆解时你没有依据，而定期分析时你有（「这条卡了三周，
  四条别的任务在等它」）——而且它是建议，他点了才算数。
- **`patch` 不能是空对象。** 一条什么都不改的建议摆在卡上，他点「接受」什么也不会
  发生，比没有更糟。
- **`reason` 必填一句**，跟 `aiComment` 同一个道理：他要能看出你为什么这么建议，
  才判断得了该不该接受。
- **不要写 `id` 之外的任务身份字段。** 建议自己的 id 由服务生成，你不用管。
- **`id` 在 `data/tasks/` 里找不到**（他把那条任务删了）：这条建议被丢弃、记日志，
  **不算校验失败**，你不用特殊处理。
- **跑之前先读 `data/proposals/`。** 那里是你以前提过、他还没处理的建议。
  已经挂着待决建议的任务**直接跳过**，别换个措辞把同一个意见再提一遍——服务只挡得住
  一字不差的重复，换了说法的挡不住，结果就是同一件事在卡上堆成好几条。
- **`dismissed: true` 的那些是他明确点过「忽略」的。** 它们留在文件里就是为了
  挡住你再提一遍，界面上看不到。**这类更要跳过**——同一条再提等于没听他的话。
  情况真变了（又拖了一个月、期限换了），你算出来的是不一样的内容，那是一条新建议，
  照样提得出来。

## 第三种条目：`insights`（跨任务的观察）

`tasks` 说的是「这句话该拆成什么」，`updates` 说的是「这一条任务该改哪个字段」。
有些话这两种都说不出来——「你的写作类任务清一色在晚上十点后完成」「这三条其实
是同一件事」，它们不挂在任何一条任务上。那种话写这里。

```json
[
  { "insights": [
      { "kind": "pattern",
        "text": "你的写作类任务清一色在晚上十点后完成，排在白天等于骗自己",
        "taskIds": ["…", "…"] }
  ]}
]
```

`kind` 四选一：`pattern`（反复出现的规律）/ `duplicate`（这几条是一件事）/
`stuck`（卡住了）/ `note`（别的）。`text` 必填、不能是空白。`taskIds` 填相关的
任务，没有就空数组。

**跑之前先读 `data/insights/`。** `dismissedAt` 非空的是他明确点过「知道了」的，
换个措辞再提一遍等于没听他的话。

**一个条目里只能出现 `tasks` / `updates` / `insights` 中的一个。** 同时出现
两个会让整个文件被拒收——服务没法判断你想干哪件事。

## 校验失败会怎样

某个 outbox 文件里任何一个任务对象没通过校验（标题为空、`status` 不是五选一之一、
**`status` 写成了 `"later"` 或 `"abandoned"`**、时间不是合法的 ISO 字符串、`habit: true` 但 `repeat`
对不上「每天」「每周」……），**这一个文件整批都不合并**：原样留着，`data/tasks/` 一个文件都
不会碰，网页会弹一条红色横幅说明是哪个文件、第几项、为什么。改好那个字段、把整个
文件原样重写一遍就行——不用先删它，也不用管别的 outbox 文件：一个文件校验不过不
影响别的文件正常合并。

**`order`、`priority`、`completedAt`、`postponeCount`、`focusSessions`、
`attachments`、`pinned`、`parentId`、`estimateMinutes`、`reviewedAt`、`section`、
`persistentReminder` 这些字段不在这条规则里**——不管你在任务对象里怎么写它们，服务合并
的时候都会悄悄改成固定值（`order` → `null`，`priority` → `0`，
`completedAt`/`parentId`/`estimateMinutes`/`reviewedAt`/`section` → `null`，
`postponeCount` → `0`，`focusSessions`/`attachments` → `[]`，
`pinned`/`persistentReminder` → `false`），文件照样正常合并，见上面「拆解要做的事」一节。跟 `status:'later'`/`'abandoned'`、`habit` 跟 `repeat` 对不上
这两种用的是不一样的处理方式：这些字段你没有任何正当理由要填值，改掉就是纠正，
不用退回来重试；后两者是「AI 手滑写出来看起来完全正常」的错误，悄悄改掉会把它藏起来，
所以选择拒收，让你看到、改掉、重来。

这个「`data/tasks/` 一个文件都不会碰」的承诺只在**校验没通过**这一种情况下成立。校验
全部通过、真正落盘那一步反而出了意外（磁盘满了、临时文件被别的程序锁住之类，这种情况
极少见但不是不可能），横幅会用不一样的措辞说清楚是「落盘/清理时出了问题」而不是「没
通过校验」，`data/tasks/` 这时候可能已经被改了——按横幅提示确认，不用去改一个其实没
问题的字段。

`inboxId` 在 `data/inbox/` 里找不到（他在你跑的时候把那条收件箱记录删了）：任务照样
入库，只是标不到来源那一条——这不算校验失败，你不用管这种情况，服务会自己处理并记日志。

## 同一条收件箱记录不会被拆两次

服务合并的时候会检查：这条 `inboxId` 在 `data/inbox/` 里是不是已经 `processed: true`
了。是的话整条 outbox 记录会被跳过，一个任务都不会重复入库——这是防呆，不是你需要
主动配合的规则，正常工作流程里你不会碰到它。会撞上它的场景是：他在网页上点了「立即
拆解」，同时你也在终端里跑 `/expand`，两边前后脚各写了一份 outbox，都想处理同一条。
跳过的那份不算错，只是白跑一次。

## 「重新拆解」——他可能会带一句「哪儿不对」

网页收件箱侧栏「已拆解」区里每一条都有个「重新拆解」按钮。点开是一个输入框，他可以
写一句要求（「拆得太粗，按周分开」），也可以留空直接重拆。点下去之后服务会一趟做完：

- 上一轮从这条拆出来的任务**搬进垃圾箱**（软删，他捞得回来）
- 他那句要求**追加到这条记录的 `text` 后面**，形如
  `补充要求（第 2 轮）：拆得太粗，按周分开`，一轮一行
- `processed` 变回 `false`、`taskIds` 清空

所以下次你读 `data/inbox/` 看到的是一条「还没拆」的记录，**原话后面多了几行补充要求**。
正常处理就行，没有任何特殊判断要做——但那几行是他明确提的意见，**按最后一轮的要求
来拆**，别当成原话的一部分照抄成任务标题。

轮次从 2 起：第一次拆解算第 1 轮。

## 定期分析

他会定期让你回来看一遍。除了拆新的收件箱条目，还值得做这几件：

- **过期没做完的**（`due` 过了、`status` 不是 `done`）：问问是不是要改期或者拆得更小
- **在 `doing` 里躺很久的**：多半是拆得太粗，一步做不完
- **反复出现的同类任务、几条其实是一件事、明显卡住了**：这几件挂不到单独一条任务上，
  说不清「该改哪个字段」，用 `insights` 提出来（见上面「第三种条目：insights」），
  不用硬塞进 `updates`

这几件都是**你说、他决定**——「你说」有两个正式出口：能落到单条任务上的字段改动写
`updates`，变成卡片上的一条建议；说不清挂在哪条任务上的观察写 `insights`。两种都
不会直接改数据，他点了才算数。见上面「提修改建议」和「第三种条目：insights」。
不要自作主张删任务——那是他的数据，而且 `updates` 也删不了任务，只能改「提修改建议」那节列出的那几个字段。

## 只看某一个项目的任务

一个项目一份清单（`listId`），所以「这个项目怎么样了」就是**按 `listId` 筛一遍**。

**他在应用里点的那次可能已经带了范围。** 侧栏每份清单的 `⋯` 里有「让 AI 回顾这份
清单」，走的是同一条路，只是叫你的那句话后面会多一句「这次只回顾清单「X」（listId
为 …）」。带了那句就照它筛，规矩在 `workflows/review.md`「叫你的那句话里带了范围」
那节——那节还点了 `insights` 也要跟着收窄，它最容易漏。

下面这两条是**你自己**要只看一个项目时用的（他从终端里叫你、或者你在别的项目的
会话里想查一眼这摊的进度）：

**在这个仓库的会话里**：直接读文件，`data/tasks/<id>.json` 里的 `listId` 就是判据。
清单名到 id 的对照在 `data/lists/<id>.json`。这条路的好处是你**同时看得见代码**——
判断「做到哪了」靠的正是这一点，光有任务列表答不上来。

**在别的项目的会话里**（够不着这个仓库的 `data/`）：走只读接口，服务默认在 30035：

```bash
# 先拿清单 id
curl -s http://127.0.0.1:30035/api/lists

# 再筛出这份清单的任务，按最后修改时间倒序
curl -s http://127.0.0.1:30035/api/tasks | python -c "
import sys, json
LIST = '把清单 id 填这儿'
ts = [t for t in json.load(sys.stdin) if t.get('listId') == LIST]
for t in sorted(ts, key=lambda x: x.get('updatedAt') or '', reverse=True):
    print(t.get('updatedAt', '')[:10], t.get('status'), t.get('title'))
"
```

**任务的 `notes` 是重点，别跳过。** 他会把项目方向、当时的想法、甚至对着编码 AI 说过
的原话记在那儿（常以 `## 方向` 起头）——代码变成了什么样 git 记得住，**当时想要什么样
只有备注里有**。判断一个项目的进展要读它，不是只看标题和状态。
改它的规矩见 `workflows/review.md`「备注只提议追加」那节：只许往上加，不许重写。
